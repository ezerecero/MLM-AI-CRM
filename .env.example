# Database Connection
# Format: postgres://user:password@host:port/database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mlm_crm?schema=public"

# NextAuth.js
# Generate a secret with `openssl rand -base64 32`
NEXTAUTH_SECRET="change_me_to_a_secure_random_string"
NEXTAUTH_URL="http://localhost:3000"

# AI Providers (Get keys from respective dashboards)
OPENAI_API_KEY="sk-..."
DEEPSEEK_API_KEY="sk-..."
GROQ_API_KEY="gsk_..."

# WhatsApp Microservice
# URL where the Node.js service is running (internal docker name or localhost)
WHATSAPP_API_URL="http://localhost:3001"

# Docker Compose Variables (Optional, defaults in docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mlm_crm
